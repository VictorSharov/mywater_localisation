<!--
doc-role: reference
doc-owner: loc_audit_changelog.md (mywater_localisation repo)
doc-scope: frozen calibration history for the localization audit — durable rationale per skip/audit rule, binding operator decisions, dated changelog entries. The LIVE workflow + calibrated sub-agent prompt is loc_audit_prompt.md; this is history the sub-agent does NOT read.
-->

# Localization audit — calibration changelog

Durable-rationale only: **why** each calibration / skip rule / binding policy exists, so an operator can run any batch without re-deriving. The full per-group execution trail (sweep counts, per-batch accept/modify/reject, reject-rate %, risk-flags, `/tmp` report paths, incident narratives, per-session owner-doc bookkeeping, Lokalise-OTA boilerplate) is **frozen history, not live workflow** — relocated verbatim to:

> **Phase 0..5 execution log** (19-language sweep, 2026-05-15..2026-05-16, COMPLETE): `mywater_ios ai_reports/tasks/2026-05-17_localization_audit_phase0-5_execution_log.md`. Per-language **applied** status is authoritative in `git log -- "water/Supporting Files/Localization/"` (branch `localization`), not here.

Each entry below: `decision — why/origin — where it lives now`. New material prompt/skip/output changes still get a dated entry here (durable rationale, one line) **and** an execution-log append if they came from a sweep.

## Skip rules (§ What NOT to flag — pilot/group-calibrated false-positive patterns)

- **#1 formal V-form/honorific in formal surfaces** — pilot 1-50 flagged all «вы»; formal register is intentional on ASC / paywall hero+CTA / legal / permission / error-with-recovery / medical-educational / Siri-educational. Generalized to all T-V/honorific langs (Phase 1). Paywall-CTA stays formal even when proven ru is casual-T (`reachTheGoal` legacy outlier, no live call-sites) — codified Pre-G6 (Face A). **Revised 2026-05-30 (hybrid)** — narrowed to a legal-binding carve-out: formal register is correct only on genuinely legal text (Terms / Privacy / subscription-legal / consent, `Register: formal`); former-formal **non-legal** surfaces (App Store / paywall / permission / error / Siri) are informal T-form with a reserved tone, legacy «вы» grandfathered (not swept). See rule #8 + the 2026-05-30 (rev.) entry below.
- **#2 legacy hard line break handling** — pilot 1-50 over-flagged mismatch; current policy is stricter: user-facing values should not contain manual hard line breaks.
- **#3 straight quotes / #4 trailing period on errors / #5 legacy please-comma / #6 comment-explained lexicon / #8 simple-noun beverage / #9 comment-explained casing / #11 pure stylistic / #12 British-in-comments** — pilot 1-50 FP buckets (separate sweeps or legacy-acknowledged).
- **#7 empty (`""`) `unverified` target = untranslated, not a defect** — do NOT flag or auto-fill an empty target during audit; it awaits a translation pass / Lokalise ([CR-CORPUS-UNVERIFIED]). Supersedes the retired `|R|`-source-marker model (the original post-13-27 rule guarded English-text fallbacks under en `|R|` — incident: 18 siri/signup keys wrongly removed + restored).
- **#10 same defect en+target → flag en only** — cross-language dedup; en is the root.
- **#13 singular/plural en for multi-select** — post-13-27: fix at en source, don't pluralize target (`chooseYourGoal`).
- **#14 gendered count-caption** — post-13-27: don't flip M-sg → plural as a "fix" (reads as "multiple subjects"); needs `M`/`F` split (`sharesOfTheApp`).
- **#15 em-dash `—` U+2014** — post-Phase-0; **2026-05-16 policy: only `—` U+2014 forbidden in user-facing values (AI tell users dislike); `-` U+002D sanctioned incl. ` - ` separator; `–` U+2013 unregulated; doc-prose `—` unaffected.** Never *propose* `—` as a suggestion; replacement anchored on en (comma/period/colon/restructure; script-correct for ar/CJK). Canonical — `TRANSLATION_STYLE.md § Brand voice § Punctuation`.
- **#16 target mirrors proven-ru restructure → not drift** — G2: codifies "How to use ru as reference" as an enforceable skip (recurring sole semantic-drift FP class). Scoped to the proven `Localizable.strings` ru only (see 2026-05-17 ru caveat).
- **#17 Danish optional pre-subordinate-clause comma** — G2: sanctioned by Dansk Sprognævn ("nyt komma"); `da`-specific carve-out, not a punctuation defect.

## Audit rules (§ What to audit)

- **rule #8 T-V/honorific map (all 21) + binding OPERATOR POLICY** — Phase 1 inlined the per-language casual/formal map (Scandinavian = "skip #8 entirely, no morphological T-V"); G1 made casual-surface V-form a **confirmed project-wide defect** (enumerate per-row, no Summary-consolidation / no deferral, validator must accept). Carried ru-parity tension CLOSED Pre-G6. **2026-05-30 (hybrid): T-form default on all surfaces EXCEPT legally-binding text** (`Register: formal` carve-out — formal register correct there); the casual sweep scope is unchanged; former-formal **non-legal** surfaces are informal-T with a reserved tone, grandfather (advisory `warn`, no sweep); added an over-familiarity flag (diminutive / slang / emoji / nagging) on serious non-legal surfaces.
- **rule #9 script-appropriate punctuation** — D1/Phase-2 added the CJK clause (ja half/full-width blind spot); **G6 DELTA-G6-1 added the RTL/Arabic-script clause** (rule was CJK-only, masked by `ar.md`; structural blind spot for Phase 4 all-21 pass). Latin/Cyrillic unaffected; Devanagari integrity is a `typo` matter (`hi.md`).
- **rule #5 gendered variants** — Phase-1: identical M/F is CORRECT for genderless langs (id/ms/vi/ja/ko/zh-Hans/tr-partial); D3/Phase-2: the carve-out suppresses ONLY the gender-agreement finding, never per-variant typo/calque/semantic; G3: CJK M/F expectation **inverted** (a DIFFERING pair is the scrutiny target); G4 Delta-C: key the finding on the *defective* variant (exact-match apply on the better one is a no-op).
- **rule #2 `<target>` calque/restructure** — post-Phase-0 added RU→EN reverse-calque awareness to the `en` audit + `application`→`app` convention (`[CM-LOCALE-LINGUISTIC-CANON]`); G4 Delta-B made the ru cross-check **mandatory pre-flag** (cuts the largest weak-signal FP class at source); G5-2 lifted "sibling-language contamination is a PRIMARY defect class" from `ms.md` into the base prompt, generalized to hi/ar.

## Format / infra / constraints

- **Triple `en+ru+target` input, ru as reference (not audit target), batch 200, ru-specific block optional, weak-AI calibration profiles inlined, `Target language:`/`Input:` first lines** — Phase 1 infra for the 19-lang sweep; `loc_audit_extract.py` 4-arg signature (legacy 3-arg numeric preserved).
- **Column discipline** (§ Input format) — G2: a defect quoted from the `en` line is `lang=en` even when co-occurring with a target issue; `current` must match the `lang` column.
- **Suggestion quality bar = a new-translation's bar** (§ Important constraints) — post-AI-translation: the auditor's own `suggestion` must pass the same brand-voice / clinical-term / spoken-plausibility filter (else `—` + rationale, never protect a "technically precise" calque); G2 added constraint-binding **suggestion completeness** (no 60-char truncation on brand-freeze / placeholder / hard-line-break rows); G5-1 added a language/register-purity self-check (auditor twice self-injected wrong-language tokens into its own suggestion).
- **`info` severity dropped; explicit hard/soft constraint split** — pilot 1-50 (noise without value; binding vs soft constraints).

## Anti-regression mechanisms

- **3-stage dead-key defense** — G3 `tr` incident (auditor lowercased a key → validator blind → `loc_audit_apply.py` silently appended a dead duplicate-casing key, real typo left unfixed). Mitigated at three independent stages: Delta-1 auditor byte-for-byte key copy (§ Output format), Delta-2 validator byte-for-byte key cross-check (validator prompt), Delta-3 applier loud-fail-no-append (`--allow-append` default off). Confirmed scaling G3→G6 (0 recurrence across 4 groups); mode is dead.
- **`RU-PARITY-DEFER:` routing** — G3 Delta-5 `[tech debt]`: surfaces a latent ru-side V-leak (ru itself V-form on a casual key) without auto-applying; true root cause was a separate operator decision, **closed Pre-G6** (now a safety net only, no known-open cluster).
- **No `lang=ru` audit rows for non-ru targets** — G4 Delta-A: the ru reference column is read-only; the deterministic applier cannot act on it.
- **Phase-3 collection architecture** — audit/validator sub-agents self-write `/tmp/..._findings.md` and return only summary counts (overrides the prompt's "no file writes" for the Phase-3 collection step only; at ~139 runs, routing every table through the orchestrator risked auto-compaction loss). Operator-approved.
- **7 batches/lang** — G1: `loc_audit_extract.py` indexes by en (1352), not target (1157); a 6-batch loop silently dropped en 1201-1352 (~102 entries/lang). Correct coverage = 7 windows.

## Binding operator decisions

- **Pre-G6 ru-anchor V-form parity — CLOSED `[root cause]`** — Face B: 7 evidenced genuinely-casual ru keys swapped V→T in `ru.lproj` (`yearResultsPushTitleTemp`, `didgestSettingsTitle`, `pressOnPlusToAdd`, `drinkParamsNotionMessage`, `youShuldAddToFriend`, `drinksReorderText`, `notifSoundText`) — restores anchor trust so all 19 langs self-heal on the normal accept path; Face A: paywall-CTA codified intentionally-formal. Settings-register keys deliberately excluded (own neutral register). Canonical owner — `TRANSLATION_STYLE.md § Brand voice § Pronouns`. Rejected `[over-engineering]` non-ru casual reference infra; rejected `[tech debt]` codify-as-formal.
- **Bespoke-prompt skip-rule import** — Phase 4 em-dash incident: a bespoke (non-audit-sub-agent) cross-language/consistency prompt does NOT inherit § What NOT to flag and recommended `—` mirroring a ru anchor that itself violates #15. Lesson: any future bespoke cross-language prompt MUST explicitly carry the no-em-dash constraint or import skip rules (Phase 5 prompts already do).
- **Mass-apply over submission-affecting files** — Phase 5: a deterministic Bash applier over many `InfoPlist.strings` is auto-mode-permission-blocked (correctly; `dangerouslyDisableSandbox` does not cover that classifier). Design the applier **idempotent** (key-only match, force-target, insert-if-absent) so partial Edit-tool progress + a later operator-run full pass compose cleanly. `mywater_ios [CR-SUBMISSION]`: Phase 5 changed localized permission *purpose* strings + added localized `NSCalendarsUsageDescription` (20 langs); hard-gated items untouched; store-review evidence = `NEEDS_ASC`.
- **ru `.stringsdict` / `InfoPlist.strings` were OUTSIDE the 27-batch proving corpus** — Phase 5 surfaced a real ru `.stringsdict` defect (`%li разы`→`%li раза`, CLDR `other`=fractions). Basis for the 2026-05-17 ru proving-corpus caveat below; do not treat ru as an infallible correctness anchor for `.stringsdict` / `InfoPlist`.

**Status:** 19-language audit **Phase 0..5 COMPLETE** (per the execution log). RU-PARITY-DEFER is a safety net only. Phase-3 collection-architecture & 7-batch coverage corrections are reflected in `ai_reports/tasks/2026-05-15_localization_audit_19_langs_plan.md`.

- **2026-05-31 — scoped ar/hi audit, ms brand decision, + CORRECTION of the `text1_9` clinical-calque rejection (operator-driven, sub-agent-validated):**
  - **CORRECTION (reverses the `text1_9` rejection in the `pl`-profile entry below).** That entry recorded `text1_9` (`bilans wodny`) as a non-defect ("faithful to en, context-sanctioned per skip #6"). **That was wrong — now reversed.** Re-adjudicated against the human canon + the proven `ru` anchor + an independent native-pl/hi sub-agent: on a casual motivational surface `bilans wodny` (en "Keep up your **water balance**!") is a **clinical calque**. `TRANSLATION_STYLE.md § Translation discipline` Принцип #3 bans `водный баланс` **+ equivalents in other languages даже если технически правильно** — so "faithful to en" is the *overridden* defense, not a valid one. `ru` proves the surface split: clinical `Водный баланс` on the **feature label** (`waterBalance`) + **store** (`appstore_app_title`), but **plain** in all 5 motivation/push strings (`text1_9`, `pushRetention_3/4/10/15`). **Root error:** skip #6 needs an explicit *preserve* directive (its example: "preserve `norm` legacy phrasing"); the `text1_9` comment merely *glosses* the en term (`"water balance" is app terminology…`) and is tagged `Register: casual T-form` — a denotation-gloss is **not** a skip-#6 sanction. **The operative test is surface register, not literal en-fidelity or a meaning-gloss.**
  - **Applied (plain, casual T-form; `loc_apply_lang` → `dirty`+`unverified`, pending operator push):** pl `text1_9M/F`=`Pij wodę regularnie!`, `pushRetention_3/4/10/15` de-clinicalized (mirroring ru's plain path); hi `text1_9M/F`=`पानी पीते रहो!`. `pl.md`'s calque example corrected (it had cited the wrong en "Stay hydrated!" — the verdict was right, the en citation wrong).
  - **ms brand — KEEP localized «Air saya» (decision; corrects `ms.md`).** Per `glossary.ndjson` (`My Water`) the brand is localized per-locale — 17/21 locales localize the wordmark; ar/id/vi keep Latin deliberately. `ms.md` skip-rule #1 wrongly grouped `My Water` with the verbatim Apple brands → corrected to record `My Water`→`Air saya` is LOCALIZED (do not flag / do not revert to Latin). Casing standardized to the glossary-canonical **sentence-case `Air saya`** (the lone Title-case outlier `appstore_appname` → `Air saya`, mirroring `CFBundleDisplayName`). Native-Malay sub-agent confirmed keep + naturalness; glossary `ms` value was already `Air saya` (no change).
  - **Scoped LLM audit (validated by sub-agents):** **ar** — all 6 CLDR plural keys CLEAN (independent sub-agent + cross-check): 6-form completeness, case-conditioned dual (nominative `-ān` subjects / oblique `-ayn` objects), many=accusative+tanwīn. **hi** — split-ergativity CLEAN across 58 M/F pairs (identical where object-agreeing / imperative / nominal; differs where subject-agreeing). 3 non-gender hi defects found + fixed (native validator APPROVED): `achivmentTextShareVkM/F` brand `मेरा पानी`→`My Water` (matches siblings + glossary), `socialShareTextVkM/F` over-escaped `\\"`→`\"`, `text3_6M/F` gratuitous M/F divergence aligned.
  - **Profiles decision:** keep the 6; add none (ar/ko/zh clean on all deterministic markers; ja/ko/zh/tr deferred — no evidence of recurring FP).
  - **Verification:** `make lint` 0/0 over 1261 keys; working tree = `strings.ndjson` (13 keys: 8 plain + 4 hi-defect + 1 ms-casing; `text3_6F` marker-only) + `ms.md` + `pl.md` + this file. Operator: `make push` (langs hi/pl/ms). Agent ran no `--apply` ([CR-ACCESS]).

- **2026-05-31 — `pl` calibration profile added (lean, evidence-derived from a 2-batch Opus 4.8 pilot):**
  - **What** — new `loc_audit_lang_calibration/pl.md` (~100 lines, lean), the **sixth** profile after ar/hi/vi/id/ms. Wired into `loc_audit_prompt.md` (the four "inline the profile when auditing this lang" references — reframed *weak-AI-signal* → *"has a calibration profile"* and added `pl`) and the dir `README.md` (`pl` moved out of the now-13 native-grade languages; stale "Opus 4.7" → "Opus 4.8 Max").
  - **Why** — `pl` is medium-resource (not weak-signal) but **trap-dense**: 4-form CLDR plural, gendered past tense, and `ru→pl` Slavic calque (the ru-native team + the audit's ru-anchor make this project-specific). A 2-batch pilot (223 keys, Opus 4.8 as auditor, **NO** profile) confirmed: batch-1 (200 mature core keys) was near-clean (1 `warn`); batch-2 (23 **targeted** plural + legal/permission keys) surfaced **2 errors + 2 warns** — a real masculine-personal `few`-form plural bug (`howMuchPeopleUseAppPlural`) and a legal-compliance content gap (`termsSubscriptionDescription` dropped the renewal-charge clause). 4.8 Max caught all of them **unaided** ⇒ the profile is **consistency insurance + FP-guards + an explicit 4-form-plural check**, NOT a model-blindness fix — hence lean. Methodological note for future calibration: defect density lives in the plural / legal / gender zones and is near-zero in mature core copy, so **sample the hard zones, not key-order**.
  - **Profile payload (all evidence-derived, not speculative):** plural `few`(nom-pl masc-personal `-li`) ≠ `many`(gen-pl) check + legitimate-identity guards (`many`==`other`; `razy`/`dni` all-coincide — do not false-flag); `ru→pl` clinical-calque blacklist (`bilans wodny` …) + the skip-#16 caveat that a Slavic-mirror of ru can still be a pl calque; legal = suppress register but keep content/completeness checks active; permission/paywall "we + `ty`" is on-brand, not a mixed-register leak; `rejestr`/`miary` are established terms; explicit brand/proper-noun constraint outranks the ru-mirror skip #16.
  - **Findings — validated (skeptical native-pl sub-agent) → 3 applied, 2 rejected:** of 5 candidates, **3 applied** to the corpus via `loc_apply_lang.py pl` (→ `dirty`+`unverified`, pending operator push): `termsSubscriptionDescription` (AMEND — restored the dropped mandatory renewal-charge + cost legal clause, a real App Store-disclosure gap), `howMuchPeopleUseAppPlural` (ACCEPT — `few` `użytkowników zaczęło` → nom-pl masc-personal `użytkownicy zaczęli`; one/many/other already correct), `privacyPolicyandterms` (AMEND — `Warunki` → `Warunki korzystania z usługi`). **2 REJECTED as non-defects:** `appstore_app_title` (ASC store-title localization is an intentional ASO / operator decision — ru localized it too) and `text1_9F` (`bilans wodny` is the context-sanctioned app term per skip #6, faithful to the en source — not a calque; sibling `text1_9M` identical). **[⚠️ REVERSED 2026-05-31 — this `text1_9` rejection was WRONG: skip #6 needs an explicit *preserve* directive, but the comment only *glosses* the en term and is tagged `Register: casual T-form`; per canon Принцип #3 + the proven ru anchor it IS a clinical calque on a casual surface. Fixed to plain `Pij wodę regularnie!`. See the correction entry at the top of this changelog.]** The legal-clause restoration still merits a final human/legal glance before the Lokalise push (the `unverified` + operator-push gate is that checkpoint).
  - **Verification** — profile/wiring = doc-only (no `.py` changed; four prompt references + README `## ` section contract intact). Findings apply = 3 `pl` corpus keys: a snapshot-diff (`/tmp/corpus_before_apply.ndjson`) confirmed **exactly those 3 keys** changed — a concurrent uncommitted corpus edit was preserved byte-identically — and lint (placeholder + qa) is 0/0 over 1261 keys. Next profile candidates (`tr`, then `ja`/`ko`) deferred pending evidence.

- **2026-05-31 — calibration profiles: redundancy trim (editorial, NO rule/FP-data delta):**
  - **What** — `ar` / `vi` / `id` / `ms.md` compressed (−33 net lines): removed prose that merely duplicated the base prompt — generic "no grammatical gender" restatements + their toy examples → one-line pointer to **rule #5** (`vi` / `id` / `ms`); em-dash-policy restatements → one-line pointer to **rule #15** (`vi` / `id` / `ms`); and non-audit-actionable iOS-render mechanics (`ar` § Script: `semanticContentAttribute` / `NSWritingDirection` / `NSAttributedString` bidi — they describe layout code, not value auditing). `hi.md` left untouched (already lean — near-all FP-guards).
  - **Preserved verbatim** — every `## ` section header (README structure contract; 7 per file confirmed), all FP-trap guards (`hi` split-ergativity, `ar` 6-form plural + dual case-conditioning, `ms`↔`id` false-friends incl. `butuh`/`bisa`), project decisions (`ar` religious-neutrality + brand-canon + Phase-2 pilot examples, `vi` `OK`↔`Đồng ý` resolution), all loanword FP-lists, all calque examples, and the 2026-05-30 register-hybrid blocks. The vi-specific gender nuance (a *differing* M/F pair is the red flag) and the `ms` Jawi-script flag were kept, not cut.
  - **Why** — operator review of profile utility: apparent "boilerplate" was mostly load-bearing (diacritics-as-semantic for `vi`, Jawi flag for `ms`, placeholder-ordering for `ar`). Only base-prompt-duplicated prose + iOS-render mechanics were genuinely redundant. No skip/audit rule meaning changed; no pilot-calibrated FP data cut.
  - **Verification** — doc-only: no `.py` / corpus touched (`loc_qa.py` / `loc_placeholder_lint.py` lint values, not these docs); rule #9 cross-ref `ar.md § Punctuation conventions` intact (that section unchanged); README's documented section structure intact.

- **2026-05-30 — `ты`/`вы` split DEPRECATED; friendly T-form universal (operator decision; doc-only, grandfather, NO sweep) — ⚠️ REFINED SAME-DAY to the hybrid; the 2026-05-30 (rev.) entry below is the current policy (this entry is the superseded flat step, kept as history):**
  - **Why** — operator brand decision: the surface-based T-/V- split (casual «ты» / formal «вы») is retired in favour of a single friendly informal register on **every** surface. The split carried a standing formal carve-out list (App Store / paywall / legal / permission / error / Siri) the brand no longer wants.
  - **What** — `TRANSLATION_STYLE.md § Brand voice § Pronouns` rewritten: friendly T-form («ты» / du / tu / … per-language) is the **universal** register; the formal-V-form surface list is relabelled deprecated/historical (kept only to locate legacy «вы» and to stop reading it as "intentional"). Mirrored here: rule #8 generalized "casual surface" → "all surfaces"; **skip rule #1 (formal-surface carve-out) marked DEPRECATED**; the T-V map header relabels the V column as deprecated legacy-reference; the `Register: formal V-form` value is deprecated (legacy-only) — working values are `casual T-form` / `neutral`. Two formal-V-form doc examples (`youShouldDrink0Digits`, `more5MillionUsers`) flipped to `casual T-form`. Calibration updated to informal-universal: `hi.md` (आप-on-permission carve-out), `id.md` (Anda-reserved-for-legal), `ms.md` (anda formal); `ar.md` / `vi.md` already informal-default (consistency note added).
  - **Scope — doc-only, grandfather, NO sweep.** Per the operator ("старые фразы могут быть устаревшими"): legacy «вы» across the corpus — now including the former-formal surfaces — is **grandfathered**, not retranslated; no mass-apply (vendor console owns translations — `CM-LOCALE-MASS-FANOUT`). The 2026-05-16 casual-surface confirmed-defect sweep is unchanged (already done); the former-formal surfaces are **not** added to it. Only **new / source-changed / renamed** strings are bound to T-form, on every surface.
  - **Verification** — doc-only: `loc_qa.py` / `loc_placeholder_lint.py` unaffected (value hygiene, not register); no `loc_corpus.py` change (corpus round-trip unaffected); no corpus value edits in this pass.

- **2026-05-30 (rev. — hybrid: narrow legal carve-out + reserved-tone axis; doc-only, grandfather, NO sweep):**
  - **Why** — the same-day flat "T-form universal on **every** surface" overshot: it gave no mechanism to stay friendly without sliding into over-familiarity on serious surfaces, and it collapsed two independent axes (pronoun = tone) into one. Operator refinement: keep T-form as the universal default, but (a) carve out genuinely **legally-binding text** (a contract is a different speech act, not brand voice), and (b) model register as **two axes** — address-form (T default / `formal` only for legal) × tone (`playful` ↔ `reserved`), pronoun-independent.
  - **What** — `TRANSLATION_STYLE.md § Brand voice § Pronouns` gains `§ Фамильярность` (the over-familiarity ban + ru checklist) and `§ Юридический carve-out` (legal register: impersonal-first, formal-token fallback); `Register: formal` **revived** as the legal-binding register (working values `casual T-form` / `neutral` / `formal`); `Tone: reserved` added. Mirrored here: rule #8 reframed "universal" → "T default except legally-binding text" with a 3-way severity split + an over-familiarity finding; skip rule #1 reframed "deprecated" → narrow legal carve-out; the `Register:` override block defines `Register: formal`; the T-V map header + a legal carve-out routing note (ja=丁寧体 not 敬語, ko=합니다체 not 합쇼체, ar=fuṣḥā not honorific, vi=impersonal not `quý vị`, da/nb/sv=lexical only); ru V-leak detail gains the legal exception + over-familiarity flag. Calibration sync: `id.md` / `ms.md` (paywall/error-vs-legal contradictions removed, legal carve-out restored), `hi.md` (permission ≠ consent-contract distinction), `ar.md` / `vi.md` (additive legal-realization notes — honorific stays wrong even in legal).
  - **Scope — doc-only, grandfather, NO sweep.** Legacy «вы» on non-legal surfaces stays grandfathered; the 2026-05-16 casual sweep is unchanged. Only new / source-changed / renamed strings are bound (T-form on non-legal, `formal` on legal). The two flipped doc examples (`youShouldDrink0Digits` = medical-rationale, `more5MillionUsers` = marketing) stay `casual T-form` — neither is legal.
  - **Open follow-up (operator-gated, NOT done here):** there is no first-class "legal-binding" flag in the corpus — `Register: formal` coverage is sparse (the 2026-05-28 backfill targeted the casual-Type leaf set, not legal keys). Recommend a one-time `loc_apply_meta` backfill of `Register: formal` onto the legal-binding key set (Terms / Privacy / subscription-legal / consent) so the auditor's legal carve-out keys off an authoritative signal, not Surface/Type heuristics. Until then the auditor falls back to Type + Surface and resolves uncertainty toward `warn` (never force-rewrite a possibly-legal string to T-form).
  - **Verification** — doc-only: `loc_qa.py` / `loc_placeholder_lint.py` unaffected (value hygiene, not register); no `loc_corpus.py` change; no corpus value edits in this pass.

- **2026-05-28 — key-level `Register:` field introduced as write-time T-/V- anchor (no sweep; doc-only):**
  - **Why** — context-coverage analysis (audit-agent post-Phase-5 review) found register signal almost absent from the corpus (V-form: 0; T-form: 4; formal: 10; casual: 15 across 1263 keys). Top-level cause: `Tone:` (134 keys) carries affective semantics only ("encouraging", "promotional", "celebratory") and never T-/V-form, while the canon `§ Translator context` rule "Tone — only if non-default" creates a circular dependency (AI must read Surface/Type to know default tone, but Surface/Type alone doesn't disambiguate casual-with-Educational-framing tips from formal educational paragraphs). V-form leak is the largest audit finding class; closing it at write-time prevents it from re-arriving at audit time.
  - **What** — new optional comment field `Register:` (`casual T-form` / `formal V-form` / `neutral`) introduced in `TRANSLATION_STYLE.md § Translator context § Опциональные поля`; cross-referenced from `§ Brand voice § Pronouns` as the per-key write-time anchor. Audit rule #8 now treats `Register:` as an authoritative override when present (formal → skip rule #1 fully applies; casual → rule #8 binding even on ambiguous surface; neutral → suppress register findings, keep per-variant content checks) — the formal→skip-#1 branch **superseded 2026-05-30** (split deprecated; formal value is now legacy-only). Absence of `Register:` falls back to surface-based default — at field introduction the corpus held 6 keys with `Register:` (0.5%), all hand-authored on NS\*UsageDescription + two ad-hoc T-form keys; the same-day casual-Type backfill below brought coverage to 18% corpus-wide and 100% on the casual-Type leaf set ({tip, tip headline, motivational text, notification body / title, achievement description}).
  - **Scope** — doc-only at field introduction. The 2026-05-28 casual-Type backfill (227 keys) followed in the same pass and is the only mass-edit so far; further surfaces (formal carve-outs, settings-neutral) stay surface-default until evidenced. The `Register:` clause is **additive** (an extra authoritative signal when present), not a replacement of surface-based defaults.
  - **Paired doc edits (same pass):** Type vocabulary closed (audit buckets by Type equality; 306 distinct values had drifted from the original ~18-entry exemplar list — re-opening drift was breaking sibling-consistency bucketing); Placeholders rule tightened (the `Placeholders:` field must itself be in universal-form, not iOS-native — caught a real leak where `%li` had survived in the comment field while the value already used universal `[%i]`); `Educational` flagged as a V-form-leak-provoking word in casual contexts (tips); `platforms: ["other"]` literal-`%` carve-out gets explicit Constraints note convention.
  - **Verification** — `python3 -m py_compile` N/A (doc-only); `loc_qa.py` and `loc_placeholder_lint.py` unaffected (rules are about *value* hygiene, not context fields); corpus round-trip byte-identical (no `loc_corpus.py` change). The 4 broken NS* ad-SDK permissions + 2 keys with iOS-native `%li` in the `Placeholders:` comment field were fixed in the same pass via `loc_apply_meta.py` (concrete bugs, not the field-introduction change itself).

- **2026-05-17 — doc-reconciliation pass (no sweep; owner-doc drift fixes, operator-requested):**
  - **Suggest ≠ translate reconciliation** — prompt intro reworded: "NOT to translate yourself" forbids silently overwriting shipped values, NOT emitting the reviewed `suggestion` (which passes the operator + validator gate). Removes a real actionability ambiguity vs § Important constraints "suggestion quality bar = new-translation rules" — a weak model could otherwise refuse to give suggestions or, conversely, self-apply.
  - **ru proving-corpus scope caveat** — § How to use ru as reference + the `ru` input-format line now state ru is PROVEN for `Localizable.strings` only; ru `.stringsdict` / `InfoPlist.strings` were outside the 27-batch corpus (Phase 5 surfaced a real ru `.stringsdict` defect `%li разы`→`%li раза`). Caveat moved from frozen changelog history to where the anchor is actually used; scoped skip-#16's ru-mirror carve-out to the proven corpus.
  - **lint coverage boundary** — § Verify clarifies `localization_lint.py` checks only non-en `Localizable.strings`; `.stringsdict` / `widget14.strings` / `InfoPlist.strings` are NOT covered → manual check, do not over-trust a green `make verify`. No script change (documents the boundary, does not widen the lint).
  - Paired owner-doc edits (same pass, outside this file): `docs/LOCALIZATION.md` (lint-coverage boundary on the write-time validate step; en-ahead key-count model corrected — `delta` is the translation backlog and is **not** required to equal `count(|R|)`, since `|R|` also sits on keys present in all 21 locales; § Новый язык create-list +`InfoPlist.strings`), `docs/ai/DOC_ROUTING.md` (phantom `Intents.strings` / `Base.lproj/Intents.intentdefinition` localization pool — non-existent on FS — replaced with the real `widget14.strings` + `widget14.intentdefinition` widget-intent pool; new-language fanout checklist corrected to `Localizable.strings` + `Localizable.stringsdict` + `InfoPlist.strings` + `widget14.strings`).
  - No new skip/audit rule; no `.strings` / `.stringsdict` value changes; no calibration delta. Verification = `make docs-check` (doc-only, no Swift touched).

## Context-audit (translator `context` field — sibling workstream)

Durable calibration for the **`context`-field** audit (workflow + prompt: `loc_context_audit_prompt.md`; deterministic pre-lint: `loc_context_lint.py`). Distinct from the value-audit history above — this audits the translator description, not the translations. Dated entries, newest first.

- **2026-05-31 — Tier B cap-grounding via workflow (the deferred caps resolved), branch `context-audit-tooling`; 28 corpus keys, all `context`-only:**
  - **Resolves the "Deferred ~29 in-app + 2 Android-store caps" of the entry below.** Audited by a deterministic fan-out workflow (`tierb-cap-audit`: 38 agents — 7 grounders over 5-key clusters + 31 adversarial verifiers; per-key iOS/Android render-site grounding, `usage_sites` pre-injected). Outcome on the 31 deferred: **28 → relax (applied), 3 → KEEP (false positive), 0 refuted, 0 keep-with-limit.** No render site has a real hard character limit (labels wrap / `adjustsFontSizeToFitWidth`, nav-bar & notification-action titles system-truncate, store keys are external metadata, widget owned by WidgetKit) → each invented `≤N chars` was replaced with a grounded qualitative `Constraints:` line that preserves real non-numeric constraints (literal `[%]` on `offBigLabel`, trailing `?` on `notReady`, "Restore purchases" wording on `restoreButton`, do-not-translate brand on the store keys).
  - **3 FALSE POSITIVES (KEEP; do NOT re-adjudicate):** `runningManCharacter` (en "Man"), `womanFullFaceCharacter` (en "Woman"), `shortQuerySearch` (en "Please enter more than 3 characters…") — none carries a `Constraints:` line; the linter inferred a "cap" from value length / the "3 characters" prose, not from any context claim. (Companion to the 5 web/server validation FPs + `appstore_app_keywords` real-ASC-limit KEEP recorded below.)
  - **Apply discipline + auto-commit note.** Workflow agents were READ-ONLY; the 28 edits were applied agent-free via the sanctioned writer `loc_apply_meta` (only the `Constraints:` line changed per key; every other context line byte-identical), with content-based snapshot/assert/exit-code verification. IMPORTANT: this repo carries a `batch` auto-commit that picks up working-tree edits, so plain `git diff` (vs HEAD) reads empty mid-session — verify this branch's corpus work with `git diff <session-start-commit> -- strings.ndjson` instead. `make lint` 0/0 over 1261. Operator: `make push`; agent ran no `--apply` against Lokalise ([CR-ACCESS]).


- **2026-05-31 — Tier B (deep LLM audit on linter-flagged keys), branch `context-audit-tooling`; 8 corpus keys, all `context`-only:**
  - **Scope:** Tier-0 signals were cap_violations ×44, register_inconsistent ×5, placeholder_unexplained ×1. **8** edits applied; the remainder adjudicated KEEP (false positive) or DEFERRED (need iOS-Swift call-site grounding). The Tier A confirmed-FP lists (33 surface_mismatch + 2 dead: `SpecialPrie`, `ShareKit`) were NOT re-adjudicated.
  - **Screenshot-caption caps relaxed (7: `appstore_screenshot_2/4/5`, `appstore_app_screenShot1_3/2_1_new/3_1/4_1`):** all `platforms:["other"]`, `referenced: none`, `Type: screenshot caption` — marketing headlines baked into App Store screenshot images by design, not rendered by app code, so the `(≤N chars)` cap has no enforceable layout limit and the longest shipped translation already exceeds it. Per the cap rule, removed the invented numeric parenthetical only, leaving the qualitative "Keep short — fits screenshot headline on one line" guidance (caps 20/25/25/30/35/40/45 → qualitative).
  - **`signUpButton` Register added (`casual T-form`):** family `signUpButton` was mixed — `signUpButton2` ("Sign up", Settings promo-cell button) already carried `Register: T-form, friendly`, but the primary `signUpButton` ("Sign up", imperative submit-button CTA) lacked any Register line → added `casual T-form` (imperative button → must carry Register). `signUpButton1` ("Don't have an account?") LEFT as-is: a different surface (paragraph / inline-question prompt, not a button), so same-surface family consistency does not bind it (skip #5).
  - **cap_violations — linter calibration (FP categories; do NOT re-adjudicate):** only the 7 screenshot caps were real this pass. **Web/server validation caps are FALSE POSITIVES** (`short_login` ≤5, `short_password` ≤6, `long_name` ≤30, `invalid_transaction_id` ≤30, `too_short_query_param` ≤3): these keys carry NO `Constraints:` cap line — the linter derives a "cap" by misreading the "N characters" phrase in the en/Context prose (e.g. "must be at least 5 characters") as a length limit. Context is accurate (describes the validation rule); KEEP. **`appstore_app_keywords` ≤100 is the REAL ASC keywords hard limit** — KEEP (faithful; the 140-char `id` over-length is a value-layer defect, out of context-audit scope).
  - **register_inconsistent — linter noise:** of 5, 1 real fix (`signUpButton`). `signUpButton2` already had Register; `signUpButton1` is a different-surface prompt. `appstore_screnShotNew1/2/3` (marketing-claim paragraphs) consistently omit Register; the "mixed family" flag is noise from the mis-familied `appstore_screnShotNew8` (a main-screen motivational-text key sharing the prefix, which legitimately carries `casual T-form`). KEEP.
  - **placeholder_unexplained — FALSE POSITIVE (`statisticsBeverageTimeOfDaySubtitle`):** en `[%] of daily intake`; `[%]` is the Lokalise universal for a LITERAL percent sign ([CR-PLACEHOLDER]), not a runtime `[%s]/[%i]/[%.1f]` placeholder, and the context already states the value "intentionally starts with a literal '%' sign — keep as-is, do not convert to a format specifier." KEEP; the linter's placeholder check should exclude the `[%]` literal.
  - **Deferred (need a stable session + iOS-Swift grounding):** the ~29 in-app cap keys (buttons/labels/pickers — must confirm a real `singleLine`/`maxLines`/fixed-width limit before relaxing vs treating as a value defect), the 2 Android-store caps (`appstore_app_title`/`appstore_appname`, `platforms:["android"]`, `referenced: android`), and the Tier C live-but-wrong shallow pass.
  - **Verification:** disk-bytes transform + snapshot/restore + exact-match assertions (all 8 contexts == intended, `dirty_meta:["context"]` set, zero t-value change, no collateral key change) → pass; `git diff --stat` = `strings.ndjson` only, 8 lines; `make lint` 0/0 over 1261. Operator: `make push` (metadata on the 8 `dirty_meta` keys = this pass). Agent ran no `--apply` against Lokalise ([CR-ACCESS]).

- **2026-05-31 — Tier A (deterministic / boilerplate fixes), branch `context-audit-tooling`; 19 corpus keys, all `context`-only:**
  - **Pilot baseline (prior session, committed at branch HEAD):** 11 applied `context` edits carrying `dirty_meta:["context"]` — `year`, `text1_2M/2F/4M/4F`, `vibrationSound`, `or`, `signUpButton2`, `noAdText`, `notification1_1`, `notification1_2`. This Tier A pass builds on them (e.g. `month` was made parallel to the pilot-fixed `year`).
  - **notification1 boilerplate de-dup (15 keys, `notification1_10`…`_24`):** removed the fabricated `Constraints: Keep within ~80 characters to fit a single-line iOS notification banner.` line. **Verified false once against iOS code** (Tier-A discipline): the reminder body is a multi-line `UNMutableNotificationContent.body` (`DrinkReminderNotificationRequestFactory`) with `title = appName()`, an optional progress `subtitle`, and a rich interactive long-look renderer — there is **no** single-line/80-char limit anywhere. Pool confirmed = exactly 51 (`NotificationsSettings.standardNotificationTextKeys = (1...51)`), picked at random per reminder (`NotificationSchedulePlanner …randomElement()`). Only 15 of 51 carried the false line (one copy-paste pass, indices 10–24); the rest never had it, so removal made the family uniform. Per-key unique `Context:`/`Tone:` preserved exactly.
  - **text1 / text4 Surface harmonization (35 keys) — consistency de-dup, not a false-claim fix:** both families carried Surface drift. text4 split 19/19 on a trailing `(progress 100%+)` parenthetical; text1 split 4 ways after the pilot enriched 4 keys (`text1_2M/2F/4M/4F`) with a "percent == 0 — any time of day, not only the morning" clarification while 14 kept a terser (still-accurate, NOT false) line. Harmonized each family to one **pilot-blessed** canonical read verbatim from a reference key (`text1_4F` / `text4_8F`) so there is zero hand-typed divergence: text1 → "…motivational text label rotated daily through MotivationalText.swift, shown when daily progress is 0 (text1 group, percent == 0) — any time of day, not only the morning."; text4 → "…congratulatory label, shown after the user has reached or exceeded the daily water goal (progress 100%+)." **Only the Surface line changed** — per-key `Context:`/`Type:`/`Register:`/`Tone:` preserved (asserted line-by-line). Single atomic `loc_corpus.write_records` pass (56 scanned, 35 changed, rest already canonical). Note: the "morning" wording the prompt flagged was already absent from the live 14× majority (it said `percent == 0`); the pilot's "not only the morning" is a *defensive clarification*, kept in the canonical.
  - **surface-mismatch root-causing — the linter signal is HIGH-NOISE (key calibration):** 44 `surface_mismatch` flags → **2 real defects**, ~42 false positives. Future Tier B should NOT re-adjudicate these from scratch — they are false-positive because the cited symbol is legitimate non-surface grounding. Categories: **(a) VC-vs-VM same screen** — context cites `XViewController`, the `R.string` call lives in `XViewModel` of the same screen (`appIconSection*`, `caffeineDescription`, `sugarDescription`, `youShouldDrink1Digits`, `*TrackingFeedOnlyMessage`); surface correct, symbol drift only (skip #6). **(b) SDK/framework type as mechanism** — `HKQuantityTypeIdentifier` (`appleHealthPartialItem*`), `UIApplication` (`appIconChangeFailed`), `UIAccessibility` (`a11yPieChartSelected`), `StoreKit` (`watchPremiumProductUnavailable`). **(c) data-source / formatter / service ref** — `WaterAmountDisplayFormat`, `WaterNumberFormatter`, `AlertService`, `MainFeedBuilder`, `AwardDisplayFormat`/`DefaultAwards` (awards resolve **dynamically** on the Awards screen via `forNameKey:`/`forDescriptionKey:` — `AwardsViewModel`/`AwardsCellViewModel`; the direct `R.string.ach_*` sites in Onboarding are previews, so "Achievements list" surface is correct). **(d) destination ref** (screen the string OPENS, not where it renders) — `AppStyleViewController` for `customize`, `NewYearDigestViewController` for `openResults`/`newYearResultsTitle`. **(e) multi-surface / app-wide atom** — `knowMore`, `sugar`, `time`, `signUpButton2` (`AuthorizationActivity` = the real Android site of a cross-platform key). **(f) error-enum ref** — `SearchUsersErrors`, `FavoriteDrinksManager`/`AddFavoriteDrinkError`. **(g) sibling-key ref** — `TwoHours`/`FiveHours` (`hour`). **(h) a11y-id namespace** — `A11y` (`calendar`). **(i) prose mis-tokenized** — `Mail` (iOS Mail app), `FitBit` (example provider).
  - **2 real surface mismatches fixed:** `month` — cited "Statistics period picker (`BeveragesStatPeriod`, 'Current/Last month')" is **false**; `R.string.month()` renders only in the Weight chart Month/Year granularity toggle (`WeightNewViewModel.periodGranularityItems`, `WeightFullPlaceholder1View`), and the Statistics module references neither `month` nor `year` — re-pointed at the Weight toggle (+ Android paywall "/ month"), parallel to pilot-fixed `year`. `liveActivity` — cited "Settings → App Style" is **false**; renders only in `NotificationsPopupIntervalsViewController` (Settings → Notifications, `liveActivityRow`) — re-pointed.
  - **dead-citation root-causing:** 5 `dead_citations` → **2 real**, 3 false positives. **Real:** `yourHeight` + `yourAge` both cited `OnboardingHeightViewController`, but the real file is `OndoardingHeightViewController.swift` — **a typo in the iOS source itself**. Dropped the misspelled class (`yourHeight` surface → "Onboarding height screen" + real `ScaleSliderViewModel` `.height` case; `yourAge` sibling note → bare "yourHeight"). **False positives (KEEP):** `NewYearDiscountSpecialPrie`/`SpecialPrie` — "SpecialPrie" is the **key name**, which the context intentionally documents ("legacy key name has a typo … do not rename"), not a code citation. `Vegetables_and_fruits`/`R.swift` — "R.swift" is the **codegen library** (cited correctly: "R.swift exposes it as `vegetablesAndFruits()`"), mis-matched by the `*.swift`-filename regex. `facebookSharingError`/`ShareKit` — "ShareKit" = **`FBSDKShareKit`** (verified `import FBSDKShareKit`); the linter's prefix-match can't span the `FBSDK` prefix (skip #6, cosmetic — left as-is).
  - **Linter de-noising (DONE this pass):** suppressed the *mechanical* false-positive categories at source — added `UIApplication`, `UIAccessibility`, `HKQuantityTypeIdentifier`, `StoreKit`, `A11y` to `loc_context_lint.SDK_ALLOW`, and `R.swift` to a new `SWIFT_FILE_ALLOW` (the code-gen library, not a real file). Effect: surface_mismatch 41→33, dead 3→2 — the residue is now **only** the semantic categories (VC-vs-VM, destination, data-source, error-enum, sibling-key, multi-surface, prose), deliberately NOT suppressed because a global symbol-allowlist there could hide a real mismatch elsewhere; they stay LLM-adjudicated (and are the confirmed-FP list above, so Tier B need not re-derive them). `ShareKit`/`FBSDKShareKit` left flagged — a context-wording call (skip #6), not a linter matter.
  - **Verification:** 54 corpus keys edited this pass (15 notification1 + month + liveActivity + yourHeight + yourAge + 16 text1 + 19 text4), `git diff` = `context` + `dirty_meta:["context"]` only, **no `t`-value change** (asserted against a pre-edit snapshot); `make lint` 0/0 over 1261; context-lint deltas (corpus fixes + linter) dead 5→2, surface_mismatch 44→33. Operator: `make push` (metadata on every `dirty_meta` key = this pass + the 11 pilot keys). Agent ran no `--apply` ([CR-ACCESS]). Tier B (cap_violations ×44, register_inconsistent ×5, live-but-wrong residue) deferred to a fresh session.
