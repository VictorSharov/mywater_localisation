# Python venv / cache — the generator runs under a local venv with the Lokalise token.
.venv-lokalise/
__pycache__/
*.pyc
.DS_Store
# Pre-write corpus snapshots produced by loc_corpus.write_records — local-only
# safety net against accidental working-tree wipes ([CR-CORPUS-WORKTREE]).
.loc_backup/
