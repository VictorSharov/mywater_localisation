<!--
doc-role: status + execution log
doc-owner: loc_audit_status.md (mywater_localisation repo)
doc-scope: current per-language audit STATE (source of truth) + open follow-ups + the
  dated execution log for the localization value-audit & context-audit. Calibration
  rationale (skip/audit rules, binding decisions, anti-regression) is the sibling
  loc_audit_changelog.md. Neither file is read by the audit sub-agent.
-->

# Localization audit — status & execution log

> **How to read.** The **STATE table** below is the single source of truth for "is
> language X audited / applied / pushed / verified?". Do **NOT** infer current state from a
> historical entry in the execution archive — a past entry describes a moment, not now (the
> cost-blowup entry's "0 applied" was true only for that read-only workflow; the work was
> applied right after). Update the STATE table on every apply / push / verify.
>
> **Push ≠ verify.** A pushed value stays `unverified` until human / Lokalise review
> ([CR-CORPUS-UNVERIFIED]). As of **2026-06-01 `dirty` is empty corpus-wide** ⇒ every pass
> below is pushed; the per-entry "pending operator push" / "Operator: `make push`" lines are
> **historical** (true when written, now satisfied) — check `make push-dry` for the live
> backlog, do not re-push from them.
>
> **Auto-commit gotcha.** This repo auto-commits (`batch`), so `git diff` vs HEAD reads
> empty mid-session — verify a session's corpus work with
> `git diff <session-start-commit> -- strings.ndjson`.

## STATE — per-language audit status (source of truth; verified 2026-06-01)

| lang | role | last audit pass (date / vehicle) | fixes applied | pushed | review | open |
|---|---|---|---|---|---|---|
| en | source | 2026-05-31 Phase 6 (ru+en, Workflow) | 21 source-quote/calque | ✓ | n/a (source never `unverified`) | sv `pushRetention_3/10` en meaning-change → operator (cascades to 20 targets) |
| ru | co-source | 2026-05-31 Phase 6 | 42 | ✓ | unverified | — |
| pl | target (profile) | 2026-05-31 pilot + scoped | 17 + scoped | ✓ | unverified | — |
| ar | target (profile) | 2026-05-31 re-sweep salvage | 32 | ✓ | unverified | — |
| hi | target (profile) | 2026-05-31 salvage + scoped | 35 (+3) | ✓ | unverified | — |
| vi | target (profile) | 2026-05-31 salvage + glass/cup reconcile | 25 (+glass/cup) | ✓ | unverified | — |
| id | target (profile) | 2026-05-31 salvage | 22 | ✓ | unverified | — |
| ms | target (profile) | 2026-05-31 salvage + brand decision | 29 | ✓ | unverified | — |
| ja | target | 2026-05-31 salvage **+ ja-only full** | 16 → **102** | ✓ | unverified | glossary `full version` フル版→フルバージョン; `glass` グラス vs 一杯 (glossary-owner) |
| da | target | 2026-06-01 re-sweep ¹ | ✓ ¹ | ✓ | unverified | ¹ |
| de | target | 2026-06-01 re-sweep ¹ | ✓ ¹ | ✓ | unverified | ¹ |
| es | target | 2026-06-01 re-sweep ¹ | ✓ ¹ | ✓ | unverified | ¹ |
| fr | target | 2026-06-01 re-sweep ¹ | ✓ ¹ | ✓ | unverified | ¹ |
| it | target | 2026-06-01 re-sweep ¹ | ✓ ¹ | ✓ | unverified | ¹ |
| ko | target | 2026-06-01 re-sweep ¹ | ✓ ¹ | ✓ | unverified | ¹ |
| nb | target | 2026-06-01 re-sweep | ✓ ² | ✓ | unverified | — |
| nl | target | 2026-06-01 re-sweep | ✓ ² | ✓ | unverified | — |
| pt_BR | target | 2026-06-01 re-sweep | ✓ ² | ✓ | unverified | — |
| sv | target | 2026-06-01 re-sweep | ✓ ² | ✓ | unverified | en-side pushRetention (see `en` row) |
| tr | target | 2026-06-01 re-sweep | ✓ ² | ✓ | unverified | — |
| zh_CN | target | 2026-06-01 re-sweep | ✓ ² | ✓ | unverified | — |

¹ **Doc gap:** the line-53 summary + session memory state da/de/es/fr/it/ko were re-swept
2026-06-01 and clean, but **no detailed execution entry exists in this log** — counts/diffs
undocumented here. Confirm via `git log -- strings.ndjson` if a precise record is needed.
² Aggregate: **82 target-language edits across 62 keys** shared by nb/nl/pt_BR/sv/tr/zh_CN
(2026-06-01 re-sweep; see the entry in the archive).

**21-language Opus-4.8 re-sweep: COMPLETE & PUSHED.** All targets are `unverified` (AI/edited,
awaiting human / Lokalise verification — that gate is operator/Lokalise-owned and is NOT
cleared by the audit; see [CR-CORPUS-UNVERIFIED]).

## OPEN follow-ups (the only place open items live; on close → strike + link the closing pass)

- **[glossary-owner]** `ja`: `full version` フル版 → フルバージョン (corpus-dominant); `glass`
  グラス vs measure-phrase 一杯 in ~5 keys (vessel keys keep グラス). — raised 2026-05-31 ja-only.
- **[glossary-owner]** `vi`: optional `cup`=`tách` glossary term to lock the glass/cup
  distinction (deferred — would need filling across 20 langs). — raised 2026-05-31 vi-cup.
- **[operator]** Backfill `Register: formal` onto the legal-binding key set (Terms / Privacy /
  subscription-legal / consent) so the auditor's legal carve-out keys off an authoritative
  flag, not Surface/Type heuristics. — raised 2026-05-30 (rev., `loc_audit_changelog.md`).
- **[operator — en meaning-change]** `en` sv `pushRetention_3` "water balance" /
  `pushRetention_10` "replenish your water balance" — clinical on a casual push; fixing `en`
  cascades to all 20 targets ([CR-CORPUS-SOURCE-CHANGE]) → deliberately not auto-applied.
  — raised 2026-06-01 nb/nl re-sweep.
- **[doc gap]** da/de/es/fr/it/ko detailed execution entry missing from this log (STATE note ¹).

**~~CLOSED~~:** vi `glass`→`cốc` (closed 2026-05-31 vi-glass); vi `cup`→`tách` collision
(closed 2026-05-31 vi-cup); "ja under-covered → future ja-only pass warranted" (closed
2026-05-31 ja-only full audit, 102 edits).

## Execution archive (dated passes — newest first; HISTORICAL, not current state)

> Older **Phase 0..5** execution log (19-lang sweep, 2026-05-15..16, COMPLETE) lives in
> `mywater_ios ai_reports/tasks/2026-05-17_localization_audit_phase0-5_execution_log.md`;
> per-language applied status is also recoverable from `git log -- strings.ndjson`.

- **2026-06-01 — Opus-4.8 Max re-sweep of the final 6 languages (`nb` / `nl` / `pt_BR` / `sv` / `tr` / `zh_CN`), branch `context-audit-tooling`; 82 corpus value edits across 62 keys (81 target-language fixes + 1 `en` source-quote fix), `dirty`+`unverified` (the lone `en` edit is `dirty`-only — the source is never `unverified`), pending operator push.** Completes the 21-language 4.8 re-sweep (prior entries cover en/ru/pl/ar/hi/vi/id/ms/ja + da/de/es/fr/it/ko). These 6 have **no calibration profile** (per `loc_audit_lang_calibration/README.md` 4.8 has native-grade convention knowledge); the inline T-V/gender map + a lean run-scoped calibration sufficed — **no new profile added** (no recurring FP class surfaced; same call as the ja pass).
  - **Vehicle (the operator-directed lean one — ONE full-context auditor + ONE independent validator + conditional doubt-resolver per language, full 1M context, NOT a batch split).** Single dynamic Workflow, pipeline over the 6 langs, read-only sub-agents; **15 agents / ~3.29M subagent tokens** (under the ≤40-agent / ≤5M-token ceiling; scope verified via the workflow's `SCOPE:` log = 6 langs before fan-out — the guard the blowup entry below mandates). Findings tables self-written to `/tmp` (Phase-3 collection); apply orchestrator-serial (`loc_apply_lang.py` per lang), **test-applied to a `--corpus` copy first** (replaced==map size, unmatched=0, 0 cross-language collateral, `en` untouched), then live (live == verified copy); `make lint` (placeholder+qa) **0/0 over 1261** before & after; `git diff` 62 keys == apply set, each carries its lang in `unverified`+`dirty`; `dirty_meta`=0.
  - **Yield — 81 applied (nb 13, nl 12, pt_BR 14, sv 8, tr 13, zh_CN 21).** Dominant class = **clinical "water-balance" calque on casual surfaces** (`vannbalanse` / `waterbalans` / `balanço hídrico` / `vattenbalans` / `su dengesi` on motivational `text1_9` + `tip*` + the `pushRetention_*` family) where the proven `ru` anchor went plain — glossary `water balance` is feature-label/store only. **Genderless-M/F divergence fixes** (`text1_9`, `text3_4`, `text3_8` nb): nb/sv/nl/tr/zh_CN have **no user-sex agreement**, so a *DIFFERING* `*M`/`*F` pair is the defect (inverted expectation, like ja/ko) — unified, keyed on the worse variant. **Two-directional brand consistency** (nb): base brand `My Water`→`Mitt Vann` (4 permission/Spotlight keys, matches CFBundleDisplayName + 32 siblings + ru) **and the inverse** — the FROZEN product tier `My Water Premium` (glossary `translatable:false`, term_id 6911307) restored from a wrong `Mitt Vann Premium` in 4 Premium keys (ZWNJ push-padding + `[%s]` + literal quotes byte-verified preserved). Plus: tr ASO `su takipçisi`(=social follower)→`su takibi` + grammar `kaydediyorsunuz mu`→`kaydediyor musun` + V-form leaks on casual-T paywall/ASO captions (`reachTheGoal` etc.); zh_CN **CJK punctuation hygiene** (18 half-width-space-after-full-width `。/！/，/：` + ASCII-comma-mid-CJK artifacts, each corroborated by a clean sibling), plural `streakDaysTitle` `天连续`→`连续天数`, `widgetText` stale source (`通过通知中心`→`无需打开应用`); sv `newPasswordDontMatch` semantic bug (wrong-password vs fields-differ) + `Dagligt mål` glossary + a clunky Siri spoken prompt; pt_BR brand casing `My water`→`My Water`.
  - **Independent-validator value (not a rubber-stamp).** sv validator **REJECTED 2** (`pushRetention_3/10`): the `en` SOURCE itself says "water balance" / "replenish your water balance", so the faithful sv is not a target calque — moved to `en_findings` rather than desyncing sv from an unchanged source. nb validator caught the `My Water Premium` freeze **MISS** (4 keys the auditor's base-brand pass overlooked). pt_BR validator reduced an auditor over-reach (`achivmentTextShareVkM` restructure → casing-only, re-synced to the VkF sibling) and confirmed M/F gender-invariance (gender-neutral `você`+verb → identical M/F is correct). zh_CN validator caught the ASC subscription-terms space **MISS**. Anti-circularity held: the water-balance flags were corroborated on the `ru` anchor + native grounds, not glossary authority alone.
  - **`en`-side findings ([CR-CORPUS-SOURCE-CHANGE]):** (a) **STILL PENDING operator decision** (meaning-affecting → cascades to all 20 targets, deliberately not auto-applied): sv `pushRetention_3` "Keep an eye on your water balance…" + `pushRetention_10` "replenish your water balance" — clinical on a casual push; ru already plain; fixing `en` lets every target render plainly. (b) **APPLIED on operator request:** `achivmentTextShareVkM` `en` quote-structure harmonized to its VkF sibling — `\"My Water app\"!` → `\"My Water\" app!` (brand-freeze `\"My Water\"` quoted, `app` outside; now byte-identical to VkF en). MEANING-PRESERVING brand-quote/punctuation fix → no target blanking, no `ru` re-author (ru already quotes `\"Моя вода\"` with the app-word outside), `en` stays verified (`dirty`-only). The 19 non-pt_BR targets keep their own `\"…\"` structure; pt_BR's casing was already fixed this pass.
  - **Mechanical note:** the `resolved_zh_CN` row for `appstore_app_subscriptionTemsAppStore` carried escaped pipes (`\|`) in its rationale that break Markdown column-count (so `loc_audit_apply.parse_table` silently skips it) — applied instead via a corpus-derived `。 `→`。` transform (strip 3 half-width spaces after the full-width period; 228→225 chars, URL + Latin/digit-run spacing preserved). A latent fragility in the table-collection path: an escaped pipe in a *non-applied* cell still desyncs the split.
  - Operator: `make push` drains the `dirty` set (62 keys; langs touched en,nb,nl,pt_BR,sv,tr,zh_CN; `make push-dry` confirms "update 62 existing, create 0, metadata 0"). Agent ran no `--apply` against Lokalise ([CR-ACCESS] / [CR-MAKE]).

> ⚠️ **RESOLVED — see § STATE (this "0 applied" is the confusion-trap).** "0 applied / corpus untouched" in the entry below describes **only** this read-only workflow; the salvaged findings **were** applied (158/159 verified present + pushed, 2026-06-01) via the SALVAGE+APPLY entry immediately below **and** the ja-only pass. Do not read it as current state.

- **2026-05-31 — ⚠️ WORKFLOW COST BLOWUP (re-sweep of ar/hi/vi/id/ms/ja) — root-caused; binding cost/pilot guard added (`loc_audit_prompt.md § Workflow 0`).** A re-sweep workflow launched as a "1-batch pilot" actually ran the **full 6-lang × 7-batch set** and was operator-interrupted: **87 agents (42 audit + 35 verify, 0 deep), ~182M raw input (≈95% cache-read) + 1.35M output tokens**, **0 applied** (corpus untouched; reports salvaged to operator Desktop — 228 audit findings / 83 verifier-confirmed / 85 needs-decision / 15 rejected). **Two compounding bugs:** (1) the pilot scope was set via a Workflow `args` override that the script *defaulted past* when unread → it ran full scope **silently** (a pilot MUST be hard-capped in-script — `items.slice(0,1)` / `throw if >1` — never a silently-defaulting arg; and the actual launched scope must be verified before reporting "pilot running"); (2) **deep-validate fan-out was per-finding (unbounded)** — agents proportional to an uncapped discovered quantity. **Binding guard:** run **one language per workflow** (~13 agents / ~3M tokens *per language* baseline — pl 13/2.73M, ru+en 34/3.63M); **estimate agents×tokens and STATE it before launch**; **> ~40 agents or > ~5M tokens ⇒ stop + confirm**; **no unbounded per-finding fan-out** (one verifier + ≤1 deep-validator *per batch*). The prior "quality over cost" note was mis-read as licensing an uncapped run — this entry + § Workflow 0 make the ceiling explicit. Salvaged findings were then validated + applied — see the entry below.

- **2026-05-31 — re-sweep SALVAGE + APPLY (ar/hi/vi/id/ms/ja), branch context-audit-tooling; 159 corpus value edits, all `dirty`+`unverified`, pending operator push.** Recovered the interrupted run's findings from disk (**0 new audit agents**), then ran the corrected lean vehicle: **6 validators (1 per language, ~749K tokens total)** — each the final native-grade gate over its language's salvaged candidates (auditor finding + verifier verdict + en/ru/context), default-reject, non-circular anchors (beverage catalogue), spoken-plausibility test. **155 validator-approved + 4 orchestrator gendered-sibling propagations** (genderless M/F parity, validated value: `vi text3_2M`, `ms text1_9M`/`text3_11M`, `ja text1_9M`) = **159 applied** (ar 32, hi 35, vi 25, id 22, ms 29, ja 16). Apply was orchestrator-serial (`loc_apply_lang.py` per lang), **test-applied to a `--corpus` copy first** (unmatched=0, changed-set==apply-set, 0 cross-language collateral) before the live write; `make lint` (placeholder+qa) **0/0 over 1261** before & after; `git diff` changed-set == edited-set, every touched key carries its lang in `unverified`+`dirty`. **ja under-covered** (only 1/7 verify batches ran pre-interrupt; 15/40 candidates applied at a deliberately high bar, the unverified-audit remainder mostly rejected as restyle) — a future ja-only pass is warranted. **[CLOSED — done: the 2026-05-31 ja-only full audit below, 102 edits.]** Reports archived to operator Desktop. Operator: `make push` (drains the `dirty` set; note it also includes the earlier ru/en/pl Phase-6 work if not yet pushed — check `make push-dry`). Agent ran no `--apply` against Lokalise ([CR-ACCESS]). **Open follow-up surfaced by the vi validator:** `glossary.ndjson` pins `vi: glass = ly`, but the shipped corpus uses `cốc nước` 37:2 over `ly nước` — reconcile the glossary `vi`/`glass` rendering to `cốc` (separate glossary edit, not done here). **[CLOSED 2026-05-31 — see the vi-glass entry below.]**

- **2026-05-31 — ja-only full audit (Opus 4.8 Max — the "future ja-only pass" the SALVAGE entry above called for), branch context-audit-tooling; 102 corpus value edits, all `ja`, `dirty`+`unverified`, pending operator push:**
  - **Vehicle (operator directive: ONE auditor + ONE validator per language, full 1M-context, NOT a 7-batch split).** Two dynamic Workflows, Opus 4.8, read-only sub-agents; apply orchestrator-serial (`loc_apply_lang.py ja`), test-applied to a `--corpus` copy first (`replaced=102, unmatched=0`, changed-set==apply-set, 0 collateral, 0 non-ja value change) before the live write. **(1) `ja-audit-find-confirm`** (2 agents, ~365K tok): one full-context auditor over ALL 1261 keys in a SINGLE pass — deliberate, to exploit the cross-key consistency the 7-batch split is structurally blind to (the documented pl-pilot FN class) → one independent skeptical native-ja validator (107 findings → 107 accept/0 reject, but it confirmed grounding + flagged 16 doubts). **(2) `ja-doubt-resolve`** (2 agents, ~98K tok): two independent native-ja specialists re-decided the doubt-flagged calls — a phrasing/terminology resolver that **OVERRODE the auditor on 5 of 14** (en-faithfulness + motivational-family distinctiveness beat the auditor's smoothing: `text3_2`→「もうすぐゴールです！」 kept short, `text4_19`→「…明日もまた頑張りましょう！」 kept "again tomorrow" distinct from `text4_18`, `text4_12`→「ほら、…」 restored the "See," lead, `text1_5` stayed en-faithful 「水を」 not ru-broadened 何も, `text2_6` kept warm お散歩…ね), and a long-form-space resolver that kept **all 4** App-Store/legal strings UNCHANGED (their interior spaces are inter-sentence separators, not artifacts — it also caught the auditor's own inconsistency there). Orchestrator pre-calibrated both with **evidence-derived** ja guardrails (divergent-M/F hotlist, interior-space hotlist cross-checked against the zh_CN sibling, known-FP guardrails), not blind glossary obedience.
  - **Yield — 102 applied:** **44 genderless M/F pairs unified** (80 keys; ja has no grammatical gender → a *DIFFERING* `*M`/`*F` pair is the defect — every pair now byte-equal, deterministically verified), **13 standalone interior-CJK-space removals** (in-app motivational/notification/tip copy; confirmed artifacts — the zh_CN sibling omits the space in 43/48 of the same keys), and **9 lexicon/consistency** fixes: kana hygiene `出来ました→できました`, glossary-canonical `身体→体` (self-care keys; the fixed compound `身体活動` left untouched), slang/clinical de-stiffening (`ゲット→獲得しました`, `改善→よくなりました`, `ナイス→…`, over-literal `グラス一杯→一杯` on divergent pairs), and legacy `完全版→フルバージョン` ("full version" — corpus-dominant 7-key rendering, validator-confirmed).
  - **NOT changed (guardrails / discipline — surfaced, not forced):** body-copy Latin **`My Water`** kept (35+ keys — the in-body wordmark is the intentional JA-app convention; only the OS app name `CFBundleDisplayName`/`appName` localizes to 「わたしの水」, so the glossary's blanket "ja localizes My Water" is over-broad for body copy); the **6 ja plural keys are all clean** (Japanese CLDR = single `other` form, placeholders intact); plain **ですます/丁寧体 not flagged** as over-formal (it is the correct casual default) — no 敬語 leak, no em-dash, no placeholder/invisible-char defect found corpus-wide.
  - **Open follow-ups (glossary owner / operator — NOT done here):** (a) `glossary.ndjson` `full version` ja **フル版 → フルバージョン** + the lone `free_premium` outlier — reconcile to the corpus-dominant フルバージョン (validator term_recommendation). (b) `glossary.ndjson` `glass` ja **グラス** vs the corpus's natural measure-phrase **一杯**(35)/コップ(9) — same class as the vi `ly→cốc` reconciliation below; the vessel keys (`glass`/`glassOfWaterCharacter`/`shotGlass`) legitimately keep グラス, but 5 measure-phrase `グラス一杯` keys (`notification1_9/11/20/36`, `tip1`) could harmonize to 一杯 in a future sweep (left untouched — no M/F trigger; the validator flagged a stylistic mass-change there as over-edit). (c) **No `loc_audit_lang_calibration/ja.md` added** — 4.8 caught the systematic defects unaided with the evidence-derived calibration; no recurring FP class surfaced (the standing "ja candidate only if a sweep shows recurring FPs" call holds).
  - **Concurrent-edit note ([CR-CORPUS-WORKTREE]):** a *concurrent session*'s vi-glass reconciliation (entry directly below) settled mid-run; its 3 `vi` corpus edits (`shotGlass`/`pushRetention_16`/`glassOfWaterCharacter`) + its `glossary.ndjson`/`vi.md`/changelog edits were all in the working tree and **preserved byte-identically** (the ja apply reads-mutates-writes ja keys only; verified 0 collateral). They are NOT this pass's work — `make push-dry` will show ja(102) + the prior un-pushed ar/hi/id/ms/vi/ru/en/pl `dirty` + those 3 vi.
  - **Verification:** test-on-copy (`replaced=102`, `unmatched=0`, changed==apply-set, 0 collateral, vi edits byte-preserved); live apply identical; `make lint` (placeholder+qa) **0/0 over 1261** before & after; corpus round-trip byte-identical; `git diff 637d36a -- strings.ndjson` = 105 keys (102 ja mine + 3 vi concurrent), every ja key `dirty`+`unverified`. Operator: `make push` (run `make push-dry` first — it drains the full multi-language dirty set, not just this pass). Agent ran no `--apply` against Lokalise ([CR-ACCESS]).

- **2026-05-31 — vi `glass` terminology reconciled to `cốc` (CLOSES the vi-validator follow-up directly above); glossary + 3 corpus stragglers + `vi.md` calibration aligned, branch context-audit-tooling.** The follow-up recommended the fix on a *corpus-majority* basis (`cốc` 37:2). Re-derived **independently** (not majority-deference, per the operator's "don't trust AI-written docs" standing note): `cốc` is the standard-register / dictionary-headword word for a drinking glass — the neutral pan-Vietnam default, and the app has **no** Southern/HCMC targeting that would justify the Southern `ly`; the single most direct datapoint — the literal `glass` container key — is human-**verified** as `cốc`; verified-corpus consensus is **33 `cốc` : 2 `ly`** (the 2 `ly` are stragglers — `pushRetention_16` is a byte-duplicate of the `cốc` string `pushRetention_9`; `glassOfWaterCharacter` is the glass mascot, sibling of `glass`=`cốc`). `ly` survived **only** in AI-authored canon (glossary + `vi.md`) that was never cross-checked against the verified corpus — so the docs, not the corpus, were the outlier. **Applied:** glossary `glass` vi `ly`→`cốc` (`loc_glossary_apply.py`); `loc_apply_lang.py vi` on **3 keys** — `pushRetention_16` (`…một cốc nước 💧`, now byte-== `pushRetention_9`), `glassOfWaterCharacter` (`Cốc`), `shotGlass` (`ly nhỏ`→`cốc nhỏ`), all `dirty`+`unverified`; `vi.md` Units line rewritten (cốc = canonical, ly = retired Southern synonym) + all 8 illustrative `ly`→`cốc` (the calibration is read verbatim by the audit sub-agent — leaving `ly` would re-teach the drift). `make lint` 0/0 over 1261; `loc_audit_glossary.py vi` now emits `glass : "cốc"`. Operator: `make push` drains the `dirty` set ([CR-ACCESS]). **Flagged, lower-confidence:** `shotGlass`→`cốc nhỏ` is a *different* container (root-consistency call; trivial single-key revert if the operator prefers `ly nhỏ`). **Separate pre-existing defect NOT fixed here:** `glass` and `cup` both render `cốc` in vi while en (glass/cup) and ru (стакан/чашка) keep them distinct — a container-name collision needing a product call (`vi.md` suggests `tách` for the tea/coffee `cup`).

- **2026-05-31 — vi `cup` container disambiguated to `tách` (CLOSES the `glass`/`cup` collision flagged in the entry directly above), branch context-audit-tooling.** Operator confirmed the presets must stay distinct (`glass` = стакан под воду / glassware; `cup` = чашка кофе/чая). Re-derived from **the verified corpus, not the calibration doc's suggestion**: the project already renders the "Cup" / «Чашка» concept as `Tách` in the verified mascot `beverageCharacter` (and "Mug" / «Кружка» as `Cái ca` in `mugCharacter`) — only the `cup` *preset* had drifted to `cốc`, colliding with `glass`=`cốc`. **Applied:** `loc_apply_lang.py vi` → `cup` `cốc`→`tách` (lowercase, matching the preset's lowercase-noun convention; value byte-derived from the verified `beverageCharacter` "Tách"), `dirty`+`unverified`. Container vocabulary now fully distinct in vi: **`cốc`** = (water) glass [`glass`, `shotGlass`=`cốc nhỏ`, `glassOfWaterCharacter`]; **`tách`** = coffee/tea cup [`cup`, `beverageCharacter`]; **`ca`/`cái ca`** = mug [`mugCharacter`]. `vi.md` Units line expanded to the full per-container map. `make lint` 0/0. No `glossary.ndjson` "cup" term exists (the drift was corpus-only, nothing to reconcile there). **Optional follow-up:** add a glossary `cup`=`tách` (vi) term paralleling `glass` to lock the distinction for future passes — deferred (would need filling across all 20 langs; not a defect). Operator: `make push` drains `dirty`.

- **2026-05-31 — Phase 6 Opus-4.8 re-sweep — `ru` + `en` full pass (Workflow vehicle), branch `context-audit-tooling`; 62 corpus keys (42 `ru` → `dirty`+`unverified`; 21 `en` → `dirty`, source-only, never `unverified`; `termsSubscriptionDescription` in both), pending operator push:**
  - **Vehicle:** dynamic Workflow `ru-en-polish-audit` — pipeline FIND → CONFIRM → DOUBT: 8 validators per language (158-key batches, full canon read) → independent skeptical confirm per batch (reject lateral / over-edit) → dedicated per-phrase validator on flagged doubts. Opus 4.8, 34 agents, ~3.63M subagent tokens. Apply orchestrator-serial (`loc_apply_lang.py ru` then `en`), test-applied to a `--corpus` copy first (`unmatched=0`); placeholders parity-checked; `make lint` 0/0 over 1261.
  - **Yield — 64 confirmed verdicts → 60 applied / 4 rejected (lateral / per-key-context conflict / already-correct); 0 en meaning-changes (cascade-safe).** Classes: clinical/AI-tone destiff (`tip5/8/9`, `notification1_29` «пополни ряды здоровых людей»→warm), film-dub calque (`text4_13` «Ты сделала это»→«У тебя получилось»), wrong-sense (`soundMessage` «Весна» season→«Пружина» spring-coil), brand freeze/localize (`watchPremiumSubtitle` «Премиум»→`Premium`; `NSHealthUpdateUsageDescription` `My Water`→«Моя вода»), glossary consistency («пробный период», «дневная цель»), single-key gender mis-agreement (`tempFirstSteps`), legal-block mixed register (`termsSubscriptionDescription`), en RU→EN reverse-calque (`yourOwnGoal`, `ourSocialNetworks` social networks→social media, `socialShareTextVk`, `drinksReorderTitle`). Orchestrator added 5 gendered-sibling consistency propagations of already-validated values (`text3_7F`, `text4_13M`, `notifErrorF`, `text4_10M`, `text1_9F`).
  - **Em-dash policy refined (operator decision):** default replacement is now **restructure** (colon/period/comma per the `en` value, or drop the connector), NOT a mechanical hyphen swap (`-` is a fallback only). Rationale: an em-dash usually signals a loosely-built sentence; Google's developer-docs style guide prefers a colon or period over a dash for item↔description ([developers.google.com/style/dashes](https://developers.google.com/style/dashes)). `—` U+2014 stays a `loc_qa.py` ERROR; `TRANSLATION_STYLE.md § Punctuation` updated, skip rule #15 synced.

- **2026-05-31 — Phase 6 Opus-4.8 re-sweep PILOT on `pl` (Workflow vehicle), branch `context-audit-tooling`; 17 corpus value edits, all `pl`, `dirty`+`unverified` pending operator push:**
  - **Vehicle:** dynamic Workflow `loc-audit-resweep-pilot` (pipeline, read-only agents) — 7 batches (200×6 + 61), each = calibrated audit sub-agent (injection order header → `loc_audit_glossary.py pl` checklist → `pl.md` profile → prompt, all verbatim) → skeptical-native adversarial validator (refute FP / catch FN). Opus 4.8, 13 agents, ~2.73M subagent tokens. Apply was orchestrator-serial (`loc_audit_apply.py pl` + `loc_apply_lang.py pl` for the propagation), never agent-side ([CR-CORPUS-CONCURRENCY]); each finding test-applied to a `--corpus` copy first (`unmatched=0`, `added=0`) before the live write.
  - **Yield — 14 audit findings + 3 cross-batch propagation = 17.** Validator over the auditor table = accept 14 / modify 2 / reject 4. High-value confirmed: `NSUserTrackingUsageDescription` (ATT purpose-string **semantic loss** — pl had dropped the advertising-partners + performance-measurement components; compliance), the **feed-style naming bug** (4 keys), `liveActivity*` brand-localization (4 keys → «Aktywność na żywo»), and the Принцип #3 clinical-calque class (`siriShowHydrationTitle/DialogValue` `nawodnienie`→plain, `pushRetention_14` `bilans wodny`→plain — the glossary lane working as designed).
  - **1 REVERSED — `appstore_appname` (auditor FP the validator wrongly ACCEPTED; do NOT re-flag).** Auditor froze the pl brand to English `My Water Balance:` citing the key's `context` "keep untranslated as a proper noun" line. Reverted byte-identical to HEAD (`Mój Bilans Wodny: …`) on **three** independent grounds: (a) glossary canon — `My Water` is **localized** (pl `Moja woda`), `water balance` is `bilans wodny`, and there is **no** frozen `My Water Balance` term; (b) **all 20 locales localize it** (ru `Моя Вода:`, de `Meine Wasserbalance:`, fr `Mon Équilibre en Eau :`…) — freezing pl would make it the lone English outlier; (c) the **binding operator decision** keeping the direct sibling `appstore_app_title` localized ("ru localized it too"). **LESSON:** a `context` Constraints line is NOT authoritative over the glossary + sibling-locale renderings + a prior operator decision; a brand-FREEZE finding must cross-check those three before accept (the validator's missing lane).
  - **2 systemic vehicle findings (must fix before scaling to all 20):**
    - **FN class — per-batch isolation is blind to cross-key consistency.** `feed` (batch 3) renamed the pl picker label `Nowoczesny`→`Aktualności`, orphaning **3 sibling `*TrackingFeedOnlyMessage` strings** (alcohol/caffeine/sugar, batches 5 & 7) that still named the old label «w stylu Nowoczesny». No agent caught it (different batches); the orchestrator propagated all 3 (ru is internally consistent: label «Лента» = message «Лента»). A full-20 run needs an explicit **cross-batch reconciliation phase** before apply.
    - **Auditor cold-start hallucination** — batch-1 auditor emitted 2 invalid StructuredOutput calls naming **fabricated keys** before it had read the batch, then self-corrected to a clean final. Backstop held: `loc_audit_apply` is replace-only and the copy-test showed `unmatched=0` / `added=0`, so no dead key could land. Keep the replace-only + copy-precheck invariant.
  - **Observed, NOT changed (operator call):** `appstore_appname` pl is Title-Case `Mój Bilans Wodny:` vs sibling `appstore_app_title` sentence-case `Mój bilans wodny -`; ru shows the **same** Title-case-in-`appname` / sentence-in-`app_title` split, so it reads as intentional ASO styling — flagged, left untouched.
  - **Scope read for Phases 6.x:** `pl` is the best-calibrated, "cleanest" language (medium-resource, own profile) and **still** yielded 17 real fixes incl. a compliance defect and a functional bug — so the re-sweep is worthwhile across the set. A **Tier-1-narrow** scope (glossary + calque + register only) would have **MISSED** the two highest-value finds (both `semantic-drift`), so the full audit, not the narrowed lane, is the right shape. Recommend **all 20 languages, full audit, + the reconciliation phase**, language-by-language with serial apply — pending operator go-ahead (large run).
  - **Verification:** `git diff` = 17 keys / 17±, changed-set == edited-set (no collateral), all `pl` `dirty`+`unverified`; corpus `dirty` = `pl`×17 only, `dirty_meta` = 0; `make lint` (placeholder + qa) **0 error / 0 warning over 1261**. Operator: `make push` (lang `pl`). Agent ran no `--apply` against Lokalise ([CR-ACCESS]).

- **2026-05-31 — scoped ar/hi audit, ms brand decision, + CORRECTION of the `text1_9` clinical-calque rejection (operator-driven, sub-agent-validated):**
  - **CORRECTION (reverses the `text1_9` rejection in the `pl`-profile entry below).** That entry recorded `text1_9` (`bilans wodny`) as a non-defect ("faithful to en, context-sanctioned per skip #6"). **That was wrong — now reversed.** Re-adjudicated against the human canon + the proven `ru` anchor + an independent native-pl/hi sub-agent: on a casual motivational surface `bilans wodny` (en "Keep up your **water balance**!") is a **clinical calque**. `TRANSLATION_STYLE.md § Translation discipline` Принцип #3 bans `водный баланс` **+ equivalents in other languages даже если технически правильно** — so "faithful to en" is the *overridden* defense, not a valid one. `ru` proves the surface split: clinical `Водный баланс` on the **feature label** (`waterBalance`) + **store** (`appstore_app_title`), but **plain** in all 5 motivation/push strings (`text1_9`, `pushRetention_3/4/10/15`). **Root error:** skip #6 needs an explicit *preserve* directive (its example: "preserve `norm` legacy phrasing"); the `text1_9` comment merely *glosses* the en term (`"water balance" is app terminology…`) and is tagged `Register: casual T-form` — a denotation-gloss is **not** a skip-#6 sanction. **The operative test is surface register, not literal en-fidelity or a meaning-gloss.**
  - **Applied (plain, casual T-form; `loc_apply_lang` → `dirty`+`unverified`, pending operator push):** pl `text1_9M/F`=`Pij wodę regularnie!`, `pushRetention_3/4/10/15` de-clinicalized (mirroring ru's plain path); hi `text1_9M/F`=`पानी पीते रहो!`. `pl.md`'s calque example corrected (it had cited the wrong en "Stay hydrated!" — the verdict was right, the en citation wrong).
  - **ms brand — KEEP localized «Air saya» (decision; corrects `ms.md`).** Per `glossary.ndjson` (`My Water`) the brand is localized per-locale — 17/21 locales localize the wordmark; ar/id/vi keep Latin deliberately. `ms.md` skip-rule #1 wrongly grouped `My Water` with the verbatim Apple brands → corrected to record `My Water`→`Air saya` is LOCALIZED (do not flag / do not revert to Latin). Casing standardized to the glossary-canonical **sentence-case `Air saya`** (the lone Title-case outlier `appstore_appname` → `Air saya`, mirroring `CFBundleDisplayName`). Native-Malay sub-agent confirmed keep + naturalness; glossary `ms` value was already `Air saya` (no change).
  - **Scoped LLM audit (validated by sub-agents):** **ar** — all 6 CLDR plural keys CLEAN (independent sub-agent + cross-check): 6-form completeness, case-conditioned dual (nominative `-ān` subjects / oblique `-ayn` objects), many=accusative+tanwīn. **hi** — split-ergativity CLEAN across 58 M/F pairs (identical where object-agreeing / imperative / nominal; differs where subject-agreeing). 3 non-gender hi defects found + fixed (native validator APPROVED): `achivmentTextShareVkM/F` brand `मेरा पानी`→`My Water` (matches siblings + glossary), `socialShareTextVkM/F` over-escaped `\\"`→`\"`, `text3_6M/F` gratuitous M/F divergence aligned.
  - **Profiles decision:** keep the 6; add none (ar/ko/zh clean on all deterministic markers; ja/ko/zh/tr deferred — no evidence of recurring FP).
  - **Verification:** `make lint` 0/0 over 1261 keys; working tree = `strings.ndjson` (13 keys: 8 plain + 4 hi-defect + 1 ms-casing; `text3_6F` marker-only) + `ms.md` + `pl.md` + this file. Operator: `make push` (langs hi/pl/ms). Agent ran no `--apply` ([CR-ACCESS]).


---

### Context-audit sub-workstream (translator `context` field)

Durable calibration for the **`context`-field** audit (workflow + prompt: `loc_context_audit_prompt.md`; deterministic pre-lint: `loc_context_lint.py`). Distinct from the value-audit history above — this audits the translator description, not the translations. Dated entries, newest first.

- **2026-05-31 — Tier B cap-grounding via workflow (the deferred caps resolved), branch `context-audit-tooling`; 28 corpus keys, all `context`-only:**
  - **Resolves the "Deferred ~29 in-app + 2 Android-store caps" of the entry below.** Audited by a deterministic fan-out workflow (`tierb-cap-audit`: 38 agents — 7 grounders over 5-key clusters + 31 adversarial verifiers; per-key iOS/Android render-site grounding, `usage_sites` pre-injected). Outcome on the 31 deferred: **28 → relax (applied), 3 → KEEP (false positive), 0 refuted, 0 keep-with-limit.** No render site has a real hard character limit (labels wrap / `adjustsFontSizeToFitWidth`, nav-bar & notification-action titles system-truncate, store keys are external metadata, widget owned by WidgetKit) → each invented `≤N chars` was replaced with a grounded qualitative `Constraints:` line that preserves real non-numeric constraints (literal `[%]` on `offBigLabel`, trailing `?` on `notReady`, "Restore purchases" wording on `restoreButton`, do-not-translate brand on the store keys).
  - **3 FALSE POSITIVES (KEEP; do NOT re-adjudicate):** `runningManCharacter` (en "Man"), `womanFullFaceCharacter` (en "Woman"), `shortQuerySearch` (en "Please enter more than 3 characters…") — none carries a `Constraints:` line; the linter inferred a "cap" from value length / the "3 characters" prose, not from any context claim. (Companion to the 5 web/server validation FPs + `appstore_app_keywords` real-ASC-limit KEEP recorded below.)
  - **Apply discipline + auto-commit note.** Workflow agents were READ-ONLY; the 28 edits were applied agent-free via the sanctioned writer `loc_apply_meta` (only the `Constraints:` line changed per key; every other context line byte-identical), with content-based snapshot/assert/exit-code verification. IMPORTANT: this repo carries a `batch` auto-commit that picks up working-tree edits, so plain `git diff` (vs HEAD) reads empty mid-session — verify this branch's corpus work with `git diff <session-start-commit> -- strings.ndjson` instead. `make lint` 0/0 over 1261. Operator: `make push`; agent ran no `--apply` against Lokalise ([CR-ACCESS]).

- **2026-05-31 — Tier B (deep LLM audit on linter-flagged keys), branch `context-audit-tooling`; 8 corpus keys, all `context`-only:**
  - **Scope:** Tier-0 signals were cap_violations ×44, register_inconsistent ×5, placeholder_unexplained ×1. **8** edits applied; the remainder adjudicated KEEP (false positive) or DEFERRED (need iOS-Swift call-site grounding). The Tier A confirmed-FP lists (33 surface_mismatch + 2 dead: `SpecialPrie`, `ShareKit`) were NOT re-adjudicated.
  - **Screenshot-caption caps relaxed (7: `appstore_screenshot_2/4/5`, `appstore_app_screenShot1_3/2_1_new/3_1/4_1`):** all `platforms:["other"]`, `referenced: none`, `Type: screenshot caption` — marketing headlines baked into App Store screenshot images by design, not rendered by app code, so the `(≤N chars)` cap has no enforceable layout limit and the longest shipped translation already exceeds it. Per the cap rule, removed the invented numeric parenthetical only, leaving the qualitative "Keep short — fits screenshot headline on one line" guidance (caps 20/25/25/30/35/40/45 → qualitative).
  - **`signUpButton` Register added (`casual T-form`):** family `signUpButton` was mixed — `signUpButton2` ("Sign up", Settings promo-cell button) already carried `Register: T-form, friendly`, but the primary `signUpButton` ("Sign up", imperative submit-button CTA) lacked any Register line → added `casual T-form` (imperative button → must carry Register). `signUpButton1` ("Don't have an account?") LEFT as-is: a different surface (paragraph / inline-question prompt, not a button), so same-surface family consistency does not bind it (skip #5).
  - **cap_violations — linter calibration (FP categories; do NOT re-adjudicate):** only the 7 screenshot caps were real this pass. **Web/server validation caps are FALSE POSITIVES** (`short_login` ≤5, `short_password` ≤6, `long_name` ≤30, `invalid_transaction_id` ≤30, `too_short_query_param` ≤3): these keys carry NO `Constraints:` cap line — the linter derives a "cap" by misreading the "N characters" phrase in the en/Context prose (e.g. "must be at least 5 characters") as a length limit. Context is accurate (describes the validation rule); KEEP. **`appstore_app_keywords` ≤100 is the REAL ASC keywords hard limit** — KEEP (faithful; the 140-char `id` over-length is a value-layer defect, out of context-audit scope).
  - **register_inconsistent — linter noise:** of 5, 1 real fix (`signUpButton`). `signUpButton2` already had Register; `signUpButton1` is a different-surface prompt. `appstore_screnShotNew1/2/3` (marketing-claim paragraphs) consistently omit Register; the "mixed family" flag is noise from the mis-familied `appstore_screnShotNew8` (a main-screen motivational-text key sharing the prefix, which legitimately carries `casual T-form`). KEEP.
  - **placeholder_unexplained — FALSE POSITIVE (`statisticsBeverageTimeOfDaySubtitle`):** en `[%] of daily intake`; `[%]` is the Lokalise universal for a LITERAL percent sign ([CR-PLACEHOLDER]), not a runtime `[%s]/[%i]/[%.1f]` placeholder, and the context already states the value "intentionally starts with a literal '%' sign — keep as-is, do not convert to a format specifier." KEEP; the linter's placeholder check should exclude the `[%]` literal.
  - **Deferred (need a stable session + iOS-Swift grounding):** the ~29 in-app cap keys (buttons/labels/pickers — must confirm a real `singleLine`/`maxLines`/fixed-width limit before relaxing vs treating as a value defect), the 2 Android-store caps (`appstore_app_title`/`appstore_appname`, `platforms:["android"]`, `referenced: android`), and the Tier C live-but-wrong shallow pass.
  - **Verification:** disk-bytes transform + snapshot/restore + exact-match assertions (all 8 contexts == intended, `dirty_meta:["context"]` set, zero t-value change, no collateral key change) → pass; `git diff --stat` = `strings.ndjson` only, 8 lines; `make lint` 0/0 over 1261. Operator: `make push` (metadata on the 8 `dirty_meta` keys = this pass). Agent ran no `--apply` against Lokalise ([CR-ACCESS]).

- **2026-05-31 — Tier A (deterministic / boilerplate fixes), branch `context-audit-tooling`; 19 corpus keys, all `context`-only:**
  - **Pilot baseline (prior session, committed at branch HEAD):** 11 applied `context` edits carrying `dirty_meta:["context"]` — `year`, `text1_2M/2F/4M/4F`, `vibrationSound`, `or`, `signUpButton2`, `noAdText`, `notification1_1`, `notification1_2`. This Tier A pass builds on them (e.g. `month` was made parallel to the pilot-fixed `year`).
  - **notification1 boilerplate de-dup (15 keys, `notification1_10`…`_24`):** removed the fabricated `Constraints: Keep within ~80 characters to fit a single-line iOS notification banner.` line. **Verified false once against iOS code** (Tier-A discipline): the reminder body is a multi-line `UNMutableNotificationContent.body` (`DrinkReminderNotificationRequestFactory`) with `title = appName()`, an optional progress `subtitle`, and a rich interactive long-look renderer — there is **no** single-line/80-char limit anywhere. Pool confirmed = exactly 51 (`NotificationsSettings.standardNotificationTextKeys = (1...51)`), picked at random per reminder (`NotificationSchedulePlanner …randomElement()`). Only 15 of 51 carried the false line (one copy-paste pass, indices 10–24); the rest never had it, so removal made the family uniform. Per-key unique `Context:`/`Tone:` preserved exactly.
  - **text1 / text4 Surface harmonization (35 keys) — consistency de-dup, not a false-claim fix:** both families carried Surface drift. text4 split 19/19 on a trailing `(progress 100%+)` parenthetical; text1 split 4 ways after the pilot enriched 4 keys (`text1_2M/2F/4M/4F`) with a "percent == 0 — any time of day, not only the morning" clarification while 14 kept a terser (still-accurate, NOT false) line. Harmonized each family to one **pilot-blessed** canonical read verbatim from a reference key (`text1_4F` / `text4_8F`) so there is zero hand-typed divergence: text1 → "…motivational text label rotated daily through MotivationalText.swift, shown when daily progress is 0 (text1 group, percent == 0) — any time of day, not only the morning."; text4 → "…congratulatory label, shown after the user has reached or exceeded the daily water goal (progress 100%+)." **Only the Surface line changed** — per-key `Context:`/`Type:`/`Register:`/`Tone:` preserved (asserted line-by-line). Single atomic `loc_corpus.write_records` pass (56 scanned, 35 changed, rest already canonical). Note: the "morning" wording the prompt flagged was already absent from the live 14× majority (it said `percent == 0`); the pilot's "not only the morning" is a *defensive clarification*, kept in the canonical.
  - **surface-mismatch root-causing — the linter signal is HIGH-NOISE (key calibration):** 44 `surface_mismatch` flags → **2 real defects**, ~42 false positives. Future Tier B should NOT re-adjudicate these from scratch — they are false-positive because the cited symbol is legitimate non-surface grounding. Categories: **(a) VC-vs-VM same screen** — context cites `XViewController`, the `R.string` call lives in `XViewModel` of the same screen (`appIconSection*`, `caffeineDescription`, `sugarDescription`, `youShouldDrink1Digits`, `*TrackingFeedOnlyMessage`); surface correct, symbol drift only (skip #6). **(b) SDK/framework type as mechanism** — `HKQuantityTypeIdentifier` (`appleHealthPartialItem*`), `UIApplication` (`appIconChangeFailed`), `UIAccessibility` (`a11yPieChartSelected`), `StoreKit` (`watchPremiumProductUnavailable`). **(c) data-source / formatter / service ref** — `WaterAmountDisplayFormat`, `WaterNumberFormatter`, `AlertService`, `MainFeedBuilder`, `AwardDisplayFormat`/`DefaultAwards` (awards resolve **dynamically** on the Awards screen via `forNameKey:`/`forDescriptionKey:` — `AwardsViewModel`/`AwardsCellViewModel`; the direct `R.string.ach_*` sites in Onboarding are previews, so "Achievements list" surface is correct). **(d) destination ref** (screen the string OPENS, not where it renders) — `AppStyleViewController` for `customize`, `NewYearDigestViewController` for `openResults`/`newYearResultsTitle`. **(e) multi-surface / app-wide atom** — `knowMore`, `sugar`, `time`, `signUpButton2` (`AuthorizationActivity` = the real Android site of a cross-platform key). **(f) error-enum ref** — `SearchUsersErrors`, `FavoriteDrinksManager`/`AddFavoriteDrinkError`. **(g) sibling-key ref** — `TwoHours`/`FiveHours` (`hour`). **(h) a11y-id namespace** — `A11y` (`calendar`). **(i) prose mis-tokenized** — `Mail` (iOS Mail app), `FitBit` (example provider).
  - **2 real surface mismatches fixed:** `month` — cited "Statistics period picker (`BeveragesStatPeriod`, 'Current/Last month')" is **false**; `R.string.month()` renders only in the Weight chart Month/Year granularity toggle (`WeightNewViewModel.periodGranularityItems`, `WeightFullPlaceholder1View`), and the Statistics module references neither `month` nor `year` — re-pointed at the Weight toggle (+ Android paywall "/ month"), parallel to pilot-fixed `year`. `liveActivity` — cited "Settings → App Style" is **false**; renders only in `NotificationsPopupIntervalsViewController` (Settings → Notifications, `liveActivityRow`) — re-pointed.
  - **dead-citation root-causing:** 5 `dead_citations` → **2 real**, 3 false positives. **Real:** `yourHeight` + `yourAge` both cited `OnboardingHeightViewController`, but the real file is `OndoardingHeightViewController.swift` — **a typo in the iOS source itself**. Dropped the misspelled class (`yourHeight` surface → "Onboarding height screen" + real `ScaleSliderViewModel` `.height` case; `yourAge` sibling note → bare "yourHeight"). **False positives (KEEP):** `NewYearDiscountSpecialPrie`/`SpecialPrie` — "SpecialPrie" is the **key name**, which the context intentionally documents ("legacy key name has a typo … do not rename"), not a code citation. `Vegetables_and_fruits`/`R.swift` — "R.swift" is the **codegen library** (cited correctly: "R.swift exposes it as `vegetablesAndFruits()`"), mis-matched by the `*.swift`-filename regex. `facebookSharingError`/`ShareKit` — "ShareKit" = **`FBSDKShareKit`** (verified `import FBSDKShareKit`); the linter's prefix-match can't span the `FBSDK` prefix (skip #6, cosmetic — left as-is).
  - **Linter de-noising (DONE this pass):** suppressed the *mechanical* false-positive categories at source — added `UIApplication`, `UIAccessibility`, `HKQuantityTypeIdentifier`, `StoreKit`, `A11y` to `loc_context_lint.SDK_ALLOW`, and `R.swift` to a new `SWIFT_FILE_ALLOW` (the code-gen library, not a real file). Effect: surface_mismatch 41→33, dead 3→2 — the residue is now **only** the semantic categories (VC-vs-VM, destination, data-source, error-enum, sibling-key, multi-surface, prose), deliberately NOT suppressed because a global symbol-allowlist there could hide a real mismatch elsewhere; they stay LLM-adjudicated (and are the confirmed-FP list above, so Tier B need not re-derive them). `ShareKit`/`FBSDKShareKit` left flagged — a context-wording call (skip #6), not a linter matter.
  - **Verification:** 54 corpus keys edited this pass (15 notification1 + month + liveActivity + yourHeight + yourAge + 16 text1 + 19 text4), `git diff` = `context` + `dirty_meta:["context"]` only, **no `t`-value change** (asserted against a pre-edit snapshot); `make lint` 0/0 over 1261; context-lint deltas (corpus fixes + linter) dead 5→2, surface_mismatch 44→33. Operator: `make push` (metadata on every `dirty_meta` key = this pass + the 11 pilot keys). Agent ran no `--apply` ([CR-ACCESS]). Tier B (cap_violations ×44, register_inconsistent ×5, live-but-wrong residue) deferred to a fresh session.


---

### Beverage catalogue — drink-name changeset (value-layer)

> Durable cross-language naming **principles** (Soda trap, anti-circularity, legacy-key≠value) are in `loc_audit_changelog.md § Beverage catalogue naming`.

- **2026-05-31 — drink-name changeset (8 edits / 5 langs), branch `context-audit-tooling`, value-layer.** Applied via `loc_apply_lang` (serial, one lang at a time): tr `Soda→Gazlı içecek`, `Boullion`(Broth) `Kemik suyu→Et suyu`; ms `Soda→Minuman ringan`, `Other→Lain-lain`; da+nb `limonade Lemonade→Limonade`; vi `Soda→Nước ngọt`, `proteinShake Sinh tố protein→Sữa lắc protein`. All `dirty+unverified` — await human-native verify (NOT operator-cleared). Grounded in **external dictionaries + app catalogue data**, never corpus self-reference. `make lint` 0/0 over 1261; push is operator (`make push`). NOT applied (operator's call): tr Title-Case normalization of the drink set; tr `Compot` `Hoşaf` vs `Kompot`/`Komposto`.
